package com.tts.eCommerce.model;

import javax.persistence.Entity;

@Entity // Let SpringBoot know that the instances of the Role class are stored in a database 
		// (We need to keep track of the roles by saving them in an H2 database to distinguish between customer, manager, administrator etc.) 

public class Role {

}
